@model IEnumerable<MoostBrand.Models.SubCategory >

@{
    ViewBag.Title = "List";
}

<a href="@Url.Action("SubCatCreate")" >Create</a>

<table>
    <tr>
        <th>
            Sub Category
        </th>
        <th>
            Category
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.Description)
            </td>
            <td>
                <ul>
                    <li>
                        <a href="@Url.Action("SubCatEdit", new { id = item.ID })" >Edit</a>
                    </li>
                    <li>
                        <a href="@Url.Action("SubCatDelete", new { id = item.ID })" >Delete</a>
                    </li>
                </ul>
            </td>
        </tr>
    }
</table>
