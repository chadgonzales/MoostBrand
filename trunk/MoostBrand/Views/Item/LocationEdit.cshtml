@model MoostBrand.Models.Location

@{
    ViewBag.Title = "Edit";
}

<a href="@Url.Action("LocationList")">Back to List</a>
<br/>

@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)

    @Html.HiddenFor(x => x.ID)

    <label>@Html.LabelFor(model => model.Description):</label>
    <div>
        @Html.EditorFor(model => model.Description)
        @Html.ValidationMessageFor(model => model.Description)
    </div>
    
    <label>@Html.LabelFor(model => model.LocationTypeID):</label>
    <div>
        @Html.DropDownList("LocationTypeID", String.Empty)
        @Html.ValidationMessageFor(model => model.LocationTypeID)
    </div>
    
    <input type="submit" value="Save"/>
}
