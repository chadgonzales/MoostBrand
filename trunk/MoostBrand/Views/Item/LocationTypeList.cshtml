@model IEnumerable<MoostBrand.Models.LocationType >

@{
    ViewBag.Title = "List";
}

<table>
    <tr>
        <th>
            Location Type
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <ul>
                    <li>
                        <a href="@Url.Action("LocationTypeEdit", new { id = item.ID })" >Edit</a>
                    </li>
                    <li>
                        <a href="@Url.Action("LocationTypeDelete", new { id = item.ID })" >Delete</a>
                    </li>
                </ul>
            </td>
        </tr>
    }
</table>
